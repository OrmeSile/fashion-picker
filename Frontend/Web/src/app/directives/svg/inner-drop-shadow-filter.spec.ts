import { InnerDropShadowFilter } from './inner-drop-shadow-filter';

describe('InnerDropShadowFilter', () => {
  it('should create an instance', () => {
    const directive = new InnerDropShadowFilter();
    expect(directive).toBeTruthy();
  });
});
